# TODO

## Ближайшее по предрелизу

- Проверить полный рабочий сценарий на предрелизной сборке `prerelease-server-build-2026-06-24`: открыть индекс, открыть неделю 25, внести правку, обновить страницу, убедиться что локальная правка сохранилась.
- Проверить, что на главной статус опубликованного отчета меняется с `Опубликованный отчет` на `Есть изменения` только после локальной правки в браузере.
- Проверить, что `Локальный черновик` появляется только для недели, которой нет в публичном `reports.json`.
- Пройти сценарий коллеги: открыть опубликованную неделю, заполнить задачи, скачать изменения/HTML из самого отчета, передать файл.
- Пройти сценарий владельца отчета: импортировать изменения коллег, принять изменения, сохранить финальный результат.
- Решить, нужна ли отдельная кнопка очистки локальной копии опубликованного отчета. Пока не добавлять, чтобы не плодить лишние кнопки.
- Для визуальных проверок использовать `tools\serve-local-preview.ps1` плюс встроенный браузер Codex. Edge headless считать запасным вариантом, а не основным способом проверки.

## Уже сделано в предрелизе

- Данные недели 25 перенесены в JSON новой схемы.
- Единый просмотрщик `report.html?data=...` открывает неделю 25.
- Изменения в опубликованном отчете сохраняются локально и не пропадают после обновления страницы.
- Статус отчета убран из самого отчета и оставлен только на главной странице.
- Автостатусы направлений пересобраны без грубой привязки `paused = требует внимания`.
- Завершенные задачи вынесены из активного списка в результаты недели или архив.

## Публикация недель

- При добавлении новой недели скачивать JSON-файл через “Подготовить публикацию”.
- Класть JSON-файл отчета в `weekly-reports/data`.
- Обновлять `weekly-reports/reports.json` вместе с JSON-файлом отчета.
- Проверять прямую ссылку на `weekly-reports/reports.json` после загрузки.
- Проверять, что ссылка из `reports.json` открывает `weekly-reports/report.html?data=<file>.json`.
- Старые автономные HTML-отчеты использовать только как архив или резерв.

## Документация и порядок работы

- Считать `demo-page-main/demo-page-main` read-only копией GitHub-состояния: читать можно, менять нельзя.
- Рабочие правки делать в корне проекта.
- Перед публикацией сверять рабочий корень с архивной GitHub-копией.
- Не удалять старые файлы из `old` и `backups` без отдельного решения.

## Сделать позже

- Проверить полный цикл новой недели по JSON-схеме на следующем реальном отчете.
- Проверить HTML-экспорт дорожной карты в обычном браузере и загрузку экспортированного файла с `assets/roadmap-builder.*` на GitHub Pages.
- Если опубликованные дорожные карты начнут расходиться по поведению, сначала чинить общий runtime `assets/fw-release-roadmap.css` и `assets/fw-release-roadmap.js`, а не копировать правки в HTML.
- После нескольких успешных JSON-публикаций оставить HTML-экспорт только как аварийный резерв.
- Добавить в интерфейс явный статус “локальная копия опубликованного отчета”, если понадобится видеть скрытые локальные дубли.
- Добавить кнопку очистки локальных копий опубликованных отчетов.
- Добавить визуальный журнал импортированных patch-файлов.
- Добавить фильтр “только измененные задачи”.
- Добавить короткую итоговую сводку после нажатия “Принять изменения”.
- Подумать, нужно ли для B2B fallback брать даты строго `> reportDate`, а не `>= reportDate`.
- Улучшить roadmap для задач с большим количеством контрольных точек.

## Не трогать сейчас

- Backend и авторизацию.
- Разделение видимости по сотрудникам.
- Полную переработку JSON-схемы.
- Встраивание годовой карты релизов внутрь недельного отчета.
- Переписывание roadmap с нуля.
