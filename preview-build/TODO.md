# TODO

## Публикация недель

- При добавлении новой недели скачивать JSON-файл через “Подготовить публикацию”.
- Класть JSON-файл отчета в `weekly-reports/data`.
- Обновлять `weekly-reports/reports.json` вместе с JSON-файлом отчета.
- Проверять прямую ссылку на `weekly-reports/reports.json` после загрузки.
- Проверять, что ссылка из `reports.json` открывает `weekly-reports/report.html?data=<file>.json`.
- Старые автономные HTML-отчеты использовать только как архив или резерв.

## Документация и порядок работы

- Считать `demo-page-main/demo-page-main` read-only копией GitHub-состояния: читать можно, менять нельзя.
- Рабочие правки делать в корне проекта.
- Перед публикацией сверять рабочий корень с архивной GitHub-копией.
- Не удалять старые файлы из `old` и `backups` без отдельного решения.

## Сделать позже

- Проверить полный цикл новой недели по JSON-схеме на следующем реальном отчете.
- Проверить HTML-экспорт дорожной карты в обычном браузере и загрузку экспортированного файла с `assets/roadmap-builder.*` на GitHub Pages.
- Если `site.html`, `release_app.html`, `heart_roadmap.html` и `neem_roadmap.html` будут активно меняться, вынести их стили и логику в общие assets по аналогии с FW roadmap.
- После нескольких успешных JSON-публикаций оставить HTML-экспорт только как аварийный резерв.
- Добавить в интерфейс явный статус “локальная копия опубликованного отчета”, если понадобится видеть скрытые локальные дубли.
- Добавить кнопку очистки локальных копий опубликованных отчетов.
- Добавить визуальный журнал импортированных patch-файлов.
- Добавить фильтр “только измененные задачи”.
- Добавить короткую итоговую сводку после нажатия “Принять изменения”.
- Подумать, нужно ли для B2B fallback брать даты строго `> reportDate`, а не `>= reportDate`.
- Улучшить roadmap для задач с большим количеством контрольных точек.

## Не трогать сейчас

- Backend и авторизацию.
- Разделение видимости по сотрудникам.
- Полную переработку JSON-схемы.
- Встраивание годовой карты релизов внутрь недельного отчета.
- Переписывание roadmap с нуля.
